const Header = () => {
    // console.log('Header has rendered')

    return (
        <header>
            <h1>Toronto's 10 Closest Provincial Parks</h1>
        </header>
    )
}

export default Header;